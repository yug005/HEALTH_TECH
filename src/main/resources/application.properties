# ===========================================
# HealthTech Demo - Application Configuration
# ===========================================

# Server Configuration
server.port=8080

# PostgreSQL Database Configuration
# Update these values according to your PostgreSQL setup
spring.datasource.url=jdbc:postgresql://localhost:5432/healthtech_db
spring.datasource.username=postgres
spring.datasource.password=yugYUG123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
# Secret key for JWT signing (Base64 encoded, min 256-bit for HMAC-SHA256)
# In production, use environment variable: ${JWT_SECRET}
jwt.secret=aGVhbHRodGVjaF9kZW1vX3NlY3JldF9rZXlfZm9yX2p3dF90b2tlbl9nZW5lcmF0aW9uXzIwMjRfc3VwZXJfc2VjdXJl
jwt.expiration=86400000

# CORS Configuration (for frontend communication)
cors.allowed-origins=http://localhost:3000,http://127.0.0.1:5500

# Logging Configuration
logging.level.com.healthtech=DEBUG
logging.level.org.springframework.security=DEBUG
